from django.shortcuts import render

# from .models import Tag

# tags = Tag.objects.all()
#
# def index(request, post_id=None):
#     return render(request, 'tags.html', {'tags': tags, 'title': 'Tags'})
